services:
  minio:
    restart: unless-stopped
    env_file: [minio.env]
    image: pgsty/minio:RELEASE.2026-06-18T00-00-00Z
    command: server /data --console-address ":9001"
    volumes:
      - /data/minio/data:/data
    labels:
      - prometheus.io/scrape=true
      - prometheus.io/port=9000
      - prometheus.io/path=/minio/v2/metrics/cluster
      - traefik.enable=true
      - traefik.http.services.ncn-minio-gui.loadbalancer.server.port=9001
      - traefik.http.routers.ncn-minio-gui.service=ncn-minio-gui
      - traefik.http.routers.ncn-minio-gui.rule=Host(`${APP_HOST:-localhost}`) && PathPrefix(`/minio/`)
      - traefik.http.middlewares.ncn-minio-gui-stripprefix.stripprefix.prefixes=/minio
      - traefik.http.routers.ncn-minio-gui.middlewares=ncn-minio-gui-stripprefix
