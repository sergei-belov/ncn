interface:
  display_name: "Docs Writer"
  short_description: "Document and validate implementation contracts"
  default_prompt: "Use $docs-writer to document the current implementation with a complete coverage checklist and validate structure, UI/UX, frontend and backend contracts, and evidence-scoped state claims."
